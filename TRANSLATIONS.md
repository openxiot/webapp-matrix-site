# 翻译状态

本站支持 11 种语言。**除英文外，全部译文由 AI 生成，未经母语者复核。**

这份文件是给未来接手的人看的：哪些是已定稿的、哪些还欠一次母语者审校、以及每种语言
需要重点检查什么。上线前的营销文案建议按语言逐个过一遍。

## 语言清单

| 语言 | locale id | URL | 书写方向 | 译文字数 | 原文状态 | 母语者复核 |
| --- | --- | --- | --- | --- | --- | --- |
| 英语 | `en` | `/`（默认语言） | LTR | 269 | **人工编写** | 不适用 |
| 简体中文 | `zh-CN` | `/zh-CN/` | LTR | 269 | **人工编写** | 不适用 |
| 西班牙语 | `es` | `/es/` | LTR | 269 | AI 生成 | ☐ 待复核 |
| 葡萄牙语（巴西） | `pt-BR` | `/pt-BR/` | LTR | 269 | AI 生成 | ☐ 待复核 |
| 德语 | `de` | `/de/` | LTR | 269 | AI 生成 | ☐ 待复核 |
| 法语 | `fr` | `/fr/` | LTR | 269 | AI 生成 | ☐ 待复核 |
| 日语 | `ja` | `/ja/` | LTR | 269 | AI 生成 | ☐ 待复核 |
| 韩语 | `ko` | `/ko/` | LTR | 269 | AI 生成 | ☐ 待复核 |
| 俄语 | `ru` | `/ru/` | LTR | 269 | AI 生成 | ☐ 待复核 |
| 意大利语 | `it` | `/it/` | LTR | 269 | AI 生成 | ☐ 待复核 |
| 阿拉伯语 | `ar` | `/ar/` | **RTL** | 269 | AI 生成 | ☐ 待复核 |

「269」是字典的叶子字符串数（`src/i18n/dicts/<locale>.ts`），不含主题层的
导航/页脚（`i18n/<locale>/docusaurus-theme-classic/`，12 条 × 10 种语言）。

## 译文的存放位置

两套机制，各管一块，**不要混**：

| 内容 | 位置 | 数量 |
| --- | --- | --- |
| 页面正文、按钮、SEO 标题描述 | `src/i18n/dicts/<locale>.ts`（`Dict` 类型） | 269 条／语言 |
| 导航栏与页脚 | `i18n/<locale>/docusaurus-theme-classic/{navbar,footer}.json` | 12 条／语言 |

默认语言是英文，根路径 `/` 直接使用 `docusaurus.config.ts` 里的英文源串，所以
**没有** `i18n/en/` 目录。导航/页脚翻译文件的**键是英文源串**（如 `item.label.Home`），
改源串就要同步改所有语言的键。

`src/i18n/dicts/index.ts` 是唯一登记语言清单的地方——加语言只需在那里加一行 import 和
一条 `dicts` 条目，`LOCALES` 会自动带上它。但字典必须写全 269 个叶子，否则
`npm run typecheck` 会失败（`Dict` 类型会检查键结构，数组长度则不会——这点仍需人工核对）。

## 复核时各语言该重点看什么

**全部语言**
- 营销语气是否符合当地习惯，而不是英文的逐字搬运。
- 短标签位（导航项、`.tag` 徽章、`mock` 里的设备名与读数）是否过长导致溢出。
  这些容器是定宽或 `nowrap` 的，德/俄/阿语最容易出问题。
- 品牌与协议名保持原样不译：Matrix、Modbus、MQTT、Matter、PLC、485、4G DTU、
  OAuth、Web、iOS、Android、APK、App Store、GitHub、Open API、app.wematrix.cc。

**德语（`de`）** —— 复合词长，名词首字母大写要一致；检查 `.tag` 与导航项是否撑破布局。
**俄语（`ru`）** —— 西里尔字母平均更宽，比德语还容易溢出；检查断行与按钮宽度。
**法语（`fr`）** —— 排版规则：`« »` 内需用不换行空格，`:` `;` `?` 前需不换行空格，
撇号用 `’` 而非 `'`。当前译文已按此处理，复核时确认没有被改回。
**日语（`ja`）** —— 营销文案的敬体程度（です・ます 调）是否符合 B2B 习惯；
注意别混入简体中文的汉字字形（已按语言分开字体栈，见 `custom.css`）。
**韩语（`ko`）** —— 합니다 体的一致性；专业术语是否用韩国业界通行说法。
**阿拉伯语（`ar`）** —— 术语（thing model、gateway、scene 等）是否用业界通用译法；
RTL 布局下箭头方向、数字与拉丁词（Modbus、MQTT）的排列是否自然。
**意大利语（`it`）** —— 全文用非正式的 `tu`，若品牌调性偏向正式需统一改为 `Lei`。
**西班牙语（`es`）** —— 当前是通用西班牙语（非拉美变体），若有拉美受众需确认用词。
**葡萄牙语（`pt-BR`）** —— 已按巴西葡语翻译，勿与欧洲葡语混用。

## 复核完成后

把上表的 ☐ 改成 ☑ 并加上复核人与日期，例如：

```
| 日语 | `ja` | `/ja/` | LTR | 269 | AI 生成 | ☑ 2026-10-03 @某人 |
```

## 机器翻译的已知局限

- 术语在同一语言内是统一的（每份字典生成时都固定了一套译法），但**跨语言的术语选择
  未经统一评审**——例如德语用 `Gerät`、法语用 `appareil` 表示 device，是否有必要
  按行业惯例调整，需要各语言各自判断。
- 长文案是意译而非直译，个别地方可能与英文原文的信息量有出入。
- 阿语、日语、韩语的营销语气尤其需要母语者过一遍——这是机器翻译最薄弱的地方。
